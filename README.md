# PyQt5 Digital Clock

A simple desktop **digital clock application** built with **Python** and **PyQt5**.
It displays the current hour, minute, and second, updating every second with a smooth UI.

---

## 📌 Features

* Real-time digital clock (HH : MM : SS)
* Auto-update every second using `QTimer`
* Clean PyQt5 interface (UI designed in Qt Designer)
* Custom window icon support

---

## 🖥️ Preview

The interface uses three number displays:

* **Hours** (`heu`)
* **Minutes** (`min`)
* **Seconds** (`sec`)

These widgets must exist in your `Ui_Form` class.

---

## 📦 Installation

### 1. Clone the project

```bash
git clone <your-repo-url>
cd <your-project-folder>
```

### 2. Install requirements

```bash
pip install PyQt5
```

---

## ▶️ Run the Application

Make sure your project contains:

* `main.py` (your code)
* `gui.py` (generated by Qt Designer `.ui` file)
* `time.ico` (window icon)

Then run:

```bash
python main.py
```

---

## 🧩 Project Structure

```
project/
│-- main.py
│-- gui.py
│-- time.ico
│-- README.md
```

---

## 🛠️ Code Overview

### Main Features:

* Inherits from **QMainWindow** and **Ui_Form**
* Starts a **QTimer** that updates every 1 second
* Displays hours, minutes, and seconds using the UI elements

---

## 📄 License

This project is open-source — feel free to modify or use it in your own applications.

---

## 🙌 Author

Created by **Oussema**.
